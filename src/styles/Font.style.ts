import { css } from 'styled-components';

export const titleFont = css`
  font-size: 18px;
  font-weight: 600;
  line-height: 28px;
  letter-spacing: -0.05em;
`;

export const subtitleFont = css`
  font-size: 16px;
  font-weight: 400;
  line-height: 24px;
  letter-spacing: -0.05em;
`;

export const innerFont = css`
  font-size: 14px;
  font-weight: 400;
  line-height: 24px;
  letter-spacing: -0.04em;
`;

export const captionFont = css`
  font-size: 13px;
  font-weight: 400;
  line-height: 20px;
  letter-spacing: -0.05em;
`;
